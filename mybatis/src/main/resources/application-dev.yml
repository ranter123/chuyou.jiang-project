#数据库连接 dev环境
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/learn?useUnicode=true&characterEncoding=utf8&useSSL=false
    driver-class-name: com.mysql.jdbc.Driver